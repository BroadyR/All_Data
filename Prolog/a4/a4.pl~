/*
NAME(S):
DATE:
COURSE:
QUARTER:
ASSIGNMENT:

*/

inv(
	item(bread),
	price(0.90),
	quantity(50)
).

inv(
	item(milk),
	price(2.50),
	quantity(30)
).

inv(
	item(cheese),
	price(1.50),
	quantity(80)
).

inv(
	item(chips),
	price(1.00),
	quantity(50)
).

inv(
	item(apples),
	price(0.30),
	quantity(100)
).

/* Add your predicates below here */

getPrice(Item,Price):- inv(Item,Price,_).

getQuantity(Item,Quantity):- inv(Item,_,Quantity).

HigherPrice(Item1,Item2):-

computeGross(Item,Gross):-

higherGross(Item1,Item2):-